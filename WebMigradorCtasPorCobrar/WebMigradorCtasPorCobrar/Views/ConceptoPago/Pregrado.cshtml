@{
    ViewBag.Title = "CONCEPTOS DE PAGO";

    string action = ViewBag.Action;
    string controller = ViewBag.Controller;
    var procedencia_value = WebMigradorCtasPorCobrar.Models.Helpers.Procedencia.Pregrado;
}

<div class="py-3">
    <h1>Pregrado <small>CONCEPTOS DE PAGO</small></h1>
</div>
<div class="row">
    <div class="col-md-2">
        <fieldset>
            <legend class="pb-3 font-weight-bold">OPCIONES</legend>
            <a class="btn btn-outline-dark btn-block my-3" id="btn-proc" href="@Url.Action("Pregrado", "ConceptoPago", new { partial = "ProcesoMigracion" })">Proceso de migración</a>
            <a class="btn btn-outline-dark btn-block my-3" id="btn-tp" href="@Url.Action("Pregrado", "ConceptoPago", new { partial = "TemporalPagos" })">Temporal de pagos</a>
            <a class="btn btn-outline-dark btn-block my-3" id="btn-mg" href="@Url.Action("Pregrado", "ConceptoPago", new { partial = "DatosMigracion" })">Registros para migración</a>
        </fieldset>
    </div>
    <div class="col-md-10">
        @if (string.IsNullOrEmpty(action))
        {
            <div class="jumbotron" style="min-height: 450px">
                <div class="card text-center">
                    <div class="card-body">
                        <h5 class="card-title">Special title treatment</h5>
                        <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
                        <a href="#" class="btn btn-primary">Go somewhere</a>
                    </div>
                </div>
            </div>
        }
        else
        {
            @Html.Action(action, controller, new { procedencia = procedencia_value })
        }
    </div>
</div>
